import { type Config, type PriceAndFees, type ProviderFees } from '@oceanprotocol/lib';
import type { Signer } from 'ethers';
import type { AccessDetails, AssetWithAccessDetails, OrderPriceAndFees } from '../../@types';
export declare function getFixedBuyPrice(accessDetails: AccessDetails, config: Config, signer: Signer): Promise<PriceAndFees>;
export declare function getOrderPriceAndFees(asset: AssetWithAccessDetails, signer: Signer, config: Config, providerFees: ProviderFees): Promise<OrderPriceAndFees>;
