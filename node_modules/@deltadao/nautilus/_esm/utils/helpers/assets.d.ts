import { type Config, type ProviderFees } from '@oceanprotocol/lib';
import type { Signer } from 'ethers';
import type { AssetWithAccessDetails, AssetWithAccessDetailsAndPrice, ComputeAsset } from '../../@types';
export declare function getAssetsWithAccessDetails(identifiers: ComputeAsset[], config: Config, signer: Signer): Promise<AssetWithAccessDetails[]>;
export declare function getAssetWithPrice(asset: AssetWithAccessDetails, signer: Signer, config: Config, providerFees: ProviderFees): Promise<AssetWithAccessDetailsAndPrice>;
